import { EditableItemGroup } from './editable-item-group';

describe('EditableItemGroup', () => {
  it('should create an instance', () => {
    expect(new EditableItemGroup()).toBeTruthy();
  });
});
