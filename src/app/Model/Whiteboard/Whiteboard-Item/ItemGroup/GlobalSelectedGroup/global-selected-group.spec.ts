import { GlobalSelectedGroup } from './global-selected-group';

describe('GlobalSelectedGroup', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new GlobalSelectedGroup()).toBeTruthy();
  });
});
