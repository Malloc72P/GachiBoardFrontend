import { SimpleDashedLineLink } from './simple-dashed-line-link';

describe('SimpleDashedLineLink', () => {
  it('should create an instance', () => {
    expect(new SimpleDashedLineLink()).toBeTruthy();
  });
});
