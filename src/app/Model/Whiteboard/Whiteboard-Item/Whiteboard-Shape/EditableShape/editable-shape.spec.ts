import { EditableShape } from './editable-shape';

describe('EditableShape', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new EditableShape()).toBeTruthy();
  });
});
