import { EditableRaster } from './editable-raster';

describe('EditableRaster', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new EditableRaster()).toBeTruthy();
  });
});
