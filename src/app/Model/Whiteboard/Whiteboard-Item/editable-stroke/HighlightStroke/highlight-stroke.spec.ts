import { HighlightStroke } from './highlight-stroke';

describe('HighlightStroke', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new HighlightStroke()).toBeTruthy();
  });
});
