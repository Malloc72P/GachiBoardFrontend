import { WhiteboardItemFactory } from './whiteboard-item-factory';

describe('WhiteboardItemFactory', () => {
  it('should create an instance', () => {
    expect(new WhiteboardItemFactory()).toBeTruthy();
  });
});
