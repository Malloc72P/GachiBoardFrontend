import { WorkHistoryManager } from './work-history-manager';

describe('WorkHistoryManager', () => {
  it('should create an instance', () => {
    expect(new WorkHistoryManager()).toBeTruthy();
  });
});
