import { EditableItemGroupDto } from './editable-item-group-dto';

describe('EditableItemGroupDto', () => {
  it('should create an instance', () => {
    expect(new EditableItemGroupDto()).toBeTruthy();
  });
});
