import { WhiteboardItemDto } from './whiteboard-item-dto';

describe('WhiteboardItemDto', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new WhiteboardItemDto()).toBeTruthy();
  });
});
