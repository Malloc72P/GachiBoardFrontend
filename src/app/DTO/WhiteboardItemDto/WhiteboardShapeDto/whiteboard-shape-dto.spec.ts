import { WhiteboardShapeDto } from './whiteboard-shape-dto';

describe('WhiteboardShapeDto', () => {
  it('should create an instance', () => {
    expect(new WhiteboardShapeDto()).toBeTruthy();
  });
});
