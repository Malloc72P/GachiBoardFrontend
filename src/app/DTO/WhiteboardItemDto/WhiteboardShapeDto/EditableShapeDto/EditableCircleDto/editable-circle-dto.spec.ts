import { EditableCircleDto } from './editable-circle-dto';

describe('EditableCircleDto', () => {
  it('should create an instance', () => {
    expect(new EditableCircleDto()).toBeTruthy();
  });
});
