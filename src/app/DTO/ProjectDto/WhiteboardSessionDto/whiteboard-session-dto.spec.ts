import { WhiteboardSessionDto } from './whiteboard-session-dto';

describe('WhiteboardSessionDto', () => {
  it('should create an instance', () => {
    expect(new WhiteboardSessionDto()).toBeTruthy();
  });
});
